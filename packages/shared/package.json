{
  "name": "@behuman/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Tipos y utilidades compartidas de beHuman (identidad + plataforma)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"(sin tests aún)\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
