{
  "name": "@behuman/issuer",
  "version": "0.0.0",
  "private": true,
  "description": "Issuer KYC mock de beHuman — firma credenciales de prueba",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"(sin tests aún)\" && exit 0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
