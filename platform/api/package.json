{
  "name": "@behuman/api",
  "version": "0.0.0",
  "private": true,
  "description": "Backend de la plataforma de opinión — feed, posts, almacenamiento de contenido",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"(sin tests aún)\" && exit 0"
  },
  "dependencies": {
    "@behuman/sdk": "*",
    "@behuman/shared": "*"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
