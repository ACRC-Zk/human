{
  "name": "@behuman/curation",
  "version": "0.0.0",
  "private": true,
  "description": "Agentes validadores (IA) + cola de moderación humana de la plataforma",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"(sin tests aún)\" && exit 0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@behuman/shared": "*"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
