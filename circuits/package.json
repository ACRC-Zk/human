{
  "name": "@behuman/circuits",
  "version": "0.0.0",
  "private": true,
  "description": "Circuito ZK (Circom) de beHuman — credencial KYC",
  "scripts": {
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/setup.sh",
    "prove": "bash scripts/prove.sh"
  },
  "dependencies": {
    "circomlib": "^2.0.5"
  },
  "devDependencies": {
    "snarkjs": "^0.7.4"
  }
}
